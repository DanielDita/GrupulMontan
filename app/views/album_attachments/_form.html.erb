<%= image_tag @album_attachment.avatar %>
<%= form_for(@album_attachment) do |f| %>
  <% if @album_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@album_attachment.errors.count, "error") %> prohibited this album_attachment from being saved:</h2>

      <ul>
      <% @album_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.number_field :album_id %>
  </div>
  <div class="field">
    <%= f.label :avatar %><br>
    <%= f.text_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
